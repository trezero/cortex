/**
 * ChatView - View orchestrator for the /chat route.
 *
 * Renders the ChatPage component which manages the three-column layout.
 */

import { ChatPage } from "../components/ChatPage";

export function ChatView() {
  return <ChatPage />;
}
